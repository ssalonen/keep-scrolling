name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  # ── Lint + guard the content-script invariants ─────────────────────────────
  # The product is two vanilla-JS content scripts, so CI is fast and Linux-only:
  # syntax-check them and run the node:test guards that lock in the CLAUDE.md
  # invariants (remove the sheet AND release the scroll-lock; match by stable
  # signatures; don't over-block faceplate-loader).
  test:
    name: Lint & invariant tests
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Set up Node
        uses: actions/setup-node@v7
        with:
          node-version: '22'

      - name: Syntax-check content scripts
        run: |
          node --check extension/block-reddit-nag.js
          node --check extension/block-x-nag.js
          node --check app/Script.js

      - name: Validate manifest.json parses
        run: node -e "JSON.parse(require('fs').readFileSync('extension/manifest.json','utf8'))"

      - name: Run invariant tests
        run: node --test

  # ── Auto-release on green main ─────────────────────────────────────────────
  # Runs only after the test job passes on a push to main. Computes the next
  # semantic version from Conventional Commits since the last release tag:
  #
  #   feat: / feat(scope):        → minor bump
  #   fix: / perf:                → patch bump
  #   feat!: / BREAKING CHANGE:   → major bump
  #   chore/docs/test/ci/etc only → NO release
  #
  # Skipping releases for non-functional commits keeps version numbers
  # meaningful. The release workflow no longer commits anything back to main
  # (the version lives only in the tag), so there is no bot commit that could
  # feed back into this job.
  compute-version:
    name: Compute release version
    needs: [test]
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    permissions:
      contents: write
    outputs:
      should_release: ${{ steps.semver.outputs.should_release }}
      new_tag: ${{ steps.semver.outputs.new_tag }}
    steps:
      - name: Checkout
        uses: actions/checkout@v7
        with:
          fetch-depth: 0  # full history + tags needed to find the last release

      - name: Compute next version from Conventional Commits
        id: semver
        run: |
          python3 - <<'PYEOF'
          import os, re, subprocess

          def sh(*args):
              return subprocess.run(args, capture_output=True, text=True).stdout.strip()

          semver_re = re.compile(r'^v?(\d+)\.(\d+)\.(\d+)$')

          def parse(v):
              m = semver_re.match(v or "")
              return tuple(int(x) for x in m.groups()) if m else (0, 0, 0)

          # Highest semver git tag, e.g. v1.2.3
          tags = sh("git", "tag", "--sort=-v:refname").splitlines()
          semver_tags = [t for t in tags if re.match(r'^v\d+\.\d+\.\d+$', t)]
          latest_tag = semver_tags[0] if semver_tags else None

          # Base version = the latest tag. Tags are the only version store now
          # (fastlane stamps the tag into the build), so the checkout above uses
          # fetch-depth: 0 to guarantee they are all present — without the full
          # history this would silently restart numbering from v0.0.0.
          base = parse(latest_tag)

          # Commits to classify: since the last tag (whole history if none).
          rng = f"{latest_tag}..HEAD" if latest_tag else "HEAD"
          log = sh("git", "log", rng, "--pretty=format:%s%x1f%b%x1e")
          entries = [e for e in log.split("\x1e") if e.strip()]

          rank = {"patch": 1, "minor": 2, "major": 3}
          feat_re = re.compile(r'^feat(\(.+?\))?:', re.I)
          fix_re = re.compile(r'^(fix|perf)(\(.+?\))?:', re.I)
          breaking_re = re.compile(r'^\w+(\(.+?\))?!:')

          chosen = None
          for e in entries:
              subject, _, body = e.partition("\x1f")
              subject = subject.strip()
              if "BREAKING CHANGE" in (subject + "\n" + body) or breaking_re.match(subject):
                  level = "major"
              elif feat_re.match(subject):
                  level = "minor"
              elif fix_re.match(subject):
                  level = "patch"
              else:
                  continue
              if chosen is None or rank[level] > rank[chosen]:
                  chosen = level

          with open(os.environ["GITHUB_OUTPUT"], "a") as out:
              if chosen is None:
                  out.write("should_release=false\n")
                  print(f"No releasable commits since {latest_tag or '(start)'} — skipping release")
              else:
                  M, m, p = base
                  if chosen == "major":
                      M, m, p = M + 1, 0, 0
                  elif chosen == "minor":
                      m, p = m + 1, 0
                  else:
                      p += 1
                  new_tag = f"v{M}.{m}.{p}"
                  out.write("should_release=true\n")
                  out.write(f"new_tag={new_tag}\n")
                  print(f"{chosen} bump from base v{base[0]}.{base[1]}.{base[2]} -> {new_tag}")
          PYEOF

      - name: Push tag
        if: steps.semver.outputs.should_release == 'true'
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git tag "${{ steps.semver.outputs.new_tag }}"
          git push origin "${{ steps.semver.outputs.new_tag }}"

  # Reuses the build/publish pipeline. Invoked via workflow_call (not the
  # tag-push trigger) because pushes made with GITHUB_TOKEN do not start new
  # workflow runs — so this is the only path that fires, and never twice.
  release:
    name: Build & release
    needs: compute-version
    if: needs.compute-version.outputs.should_release == 'true'
    uses: ./.github/workflows/release.yml
    with:
      tag: ${{ needs.compute-version.outputs.new_tag }}
    permissions:
      contents: write
    # Required: signing secrets do NOT cross workflow_call automatically. Without
    # this they all expand to "" and the release fails in fastlane.
    secrets: inherit

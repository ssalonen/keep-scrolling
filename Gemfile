# Pins the fastlane toolchain so CI and local machines resolve the same gems.
# Commit the Gemfile.lock that `bundle install` produces on first run so builds
# are reproducible (see docs/FASTLANE-MIGRATION.md).
source "https://rubygems.org"

gem "fastlane"
